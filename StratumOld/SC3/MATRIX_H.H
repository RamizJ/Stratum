//matrix small header
void InitProjectMatrix(char*path);
// Инициализируется матрицами из каталога

void SaveMatrix(char*);
// Записывает все измененные матрицы
void DoneMatrixs();
// Уничтожает менеджер матриц
void InitMatrixs();

